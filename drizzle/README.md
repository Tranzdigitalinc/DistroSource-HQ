# Migrations

Generated SQL migrations live here, committed alongside the schema change that
produced them.

This directory is intentionally empty until the production database has been
baselined — see `docs/DATABASE-MIGRATIONS.md`. Do not apply anything from here
to production until that baseline step is complete.
