# esbuild ships prebuilt binaries via a postinstall script. drizzle-kit depends
# on it, so the script must be allowed or `drizzle-kit generate` cannot run.
allowBuilds:
  esbuild: true
minimumReleaseAgeExclude:
  - '@next/*'
  - next
